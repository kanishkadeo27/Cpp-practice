#include<iostream>
using namespace std;
#include<cmath>

int main(){
    double x=3.19;
    double y=4;
    double z;

    // z= max(x,y);
    // z= min(x,y);
    // z=pow(2,3);
    // z=sqrt(9);
    // z=abs(-10);
    // z=round(x);
    z=ceil(x);
    // z=floor(x);

    cout<<z;
    return 0;
}